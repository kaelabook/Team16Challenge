package Hackathon2023;

import java.util.ArrayList;

public class Events {
    // will add information to events (ie Farmer info to Harvest)
    // what will it really do, though?
    // really?
    // anyway, this will probably send items places
    // and then get it back
    // and then send it back to main
    
    ArrayList<Events> dataBase = new ArrayList<>();

    


}
